namespace Arguments {
    export type T = number;
    export const x: Arguments.T = 0;
                    ~~~~~~~~~ [Arguments]
    export function f() {
      if (arguments.length > 0) {
        console.log('arguments should not break this rule');
      }
    }
}

[Arguments]: Qualifier is unnecessary since 'Arguments' is in scope.
